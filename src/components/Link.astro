---
// components/Link.astro
const { href, ...rest } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
// 先頭の / を取り除いてベースURLと結合する
const resolvedHref = `${baseUrl}${href.replace(/^\//, '')}`;
---
<a href={resolvedHref} {...rest}>
  <slot />
</a>